<h1>Kor Entitätstyp/CRM Class Mapping</h1>
 
<%= form_for :kind, url: kind_path(@kind), method: :put do |f| %>
 
  <p>
    <%= f.label :entitaetstyp %><br>
    <%= @kind.name %>
  </p>
 
  <p>
    <%= label_tag(:class, "CRM class:") %><br>
	<%= select_tag :class, options_from_collection_for_select(@crmClasses, :number, :fullLabel) %>
  </p>
 
  <p>
    <%= f.submit :value => "Save Mapping" %>
  </p>
 
<% end %>

<%= link_to 'Back', kinds_path %>